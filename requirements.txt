# このプロジェクトで使うライブラリの一覧
#
# Streamlit Community Cloud は、このファイルを見て自動的に
# ライブラリをインストールします。ファイル名と置き場所(リポジトリの一番上)は
# 決まっているので、変えないでください。
#
# 手元のパソコンに入れる場合:
#     pip install -r requirements.txt
#
# バージョンを固定しているのは、動作を確認した組み合わせをそのまま
# 再現するためです。新しい版を試したいときは、この数字を変えてください。

# --- ダッシュボード本体 (app.py) に必要 ---
streamlit==1.62.0
plotly==7.0.0
pandas==3.0.5

# --- データ取得スクリプトに必要 ---
requests==2.34.2          # 各APIとの通信
pyjstat==2.4.0            # Eurostat の JSON-stat 形式を表に変換する
feedparser==6.0.14        # ニュースのRSSを読み取る
deep-translator==1.11.4   # 見出しの日本語訳
